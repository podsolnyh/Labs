#include "stdafx.h"
#include <iostream>
using namespace std;
int main()
{
	float a, b, c;
	cout << "Enter real numbers:";
	cin >> a >> b >> c;
	if (a + b > c && b + c > a && a + c > b) {
		cout << "triangle exists";
	
	}
	else {
		cout << "triangle does not exist!";
	}
	
	
	
	return 0;
}

