#include <iostream>
#include <string>
#include <Windows.h>
using namespace std;
int main() {
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	string s;
	getline(cin, s);
	bool f = 1;
	int mx = 0;
	mx = max(mx, s.size());
	if (mx <= 10)f = 0;
	cout<<((f == 1) ? "Yes" : "No");
	
}
